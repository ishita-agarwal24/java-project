name: Deploy Spring Boot Application

on:
  push:
    branches:
      - main  # Change to your main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean package

      - name: Deploy to AWS
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_IP: ${{ secrets.SERVER_IP }}
          USER: "ubuntu"
        run: |
          echo "$SSH_PRIVATE_KEY" > private_key && chmod 600 private_key
          scp -i private_key target/bookstore.jar $USER@$43.204.85.137:/home/ubuntu/bookstore.jar
          ssh -i private_key $USER@$43.204.85.137 << 'EOF'
            sudo systemctl stop bookstore || true
            nohup java -jar /home/ubuntu/bookstore.jar > /home/ubuntu/logs.txt 2>&1 &
          EOF
